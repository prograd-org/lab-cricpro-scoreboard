function playerDetails(id){
    let element = document.getElementById(id);
    element.classList.toggle("show");
}
//when image is clicked adding show class which makes image visible
//when it is clicked one more time show class will be removed and by default hide class will controll visibility.